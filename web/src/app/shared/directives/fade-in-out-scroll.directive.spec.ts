import { FadeInOutScrollDirective } from './fade-in-out-scroll.directive';

describe('FadeInOutScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInOutScrollDirective();
    expect(directive).toBeTruthy();
  });
});
